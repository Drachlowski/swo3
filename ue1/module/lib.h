/*
    Author: Andreas Neubauer

    File which provides interface declarations
*/

// Include guard (if not defined = ifndef)
#ifndef LIB_H
    #define LIB_H
    int add (int a, int b);
    int sub (int a, int b);
#endif